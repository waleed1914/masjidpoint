(function(){
 const accounts=document.querySelector('#accounts'),proof=document.querySelector('.proof-queue'),settlements=document.querySelector('.settlement-panel');if(!accounts||!proof||!settlements)return;
 const tabs=document.createElement('nav');tabs.className='payment-workspace-tabs';tabs.style.cssText='display:flex;gap:0;margin:26px 0 0;background:#fffdf8;border:1px solid #c2cdc6;overflow-x:auto';tabs.innerHTML='<button data-payment-tab="accounts">Business accounts <b></b></button><button data-payment-tab="proofs">Payment proofs <b></b></button><button data-payment-tab="settlements">Mosque settlements <b></b></button>';accounts.before(tabs);const buttons=[...tabs.querySelectorAll('button')];buttons.forEach(button=>button.style.cssText='border:0;border-bottom:3px solid transparent;background:transparent;padding:16px 22px;color:#68736f;font-size:10px;font-weight:700;white-space:nowrap');let active=location.hash==='#proofs'?'proofs':location.hash==='#settlements'?'settlements':'accounts';function show(name){active=name;accounts.hidden=name!=='accounts';proof.hidden=name!=='proofs';settlements.hidden=name!=='settlements';buttons.forEach(button=>{const selected=button.dataset.paymentTab===name;button.style.borderBottomColor=selected?'#c9a253':'transparent';button.style.color=selected?'#174c3e':'#68736f';button.style.background=selected?'#f5f0e4':'transparent'});history.replaceState(null,'',name==='accounts'?'#accounts':`#${name}`)}buttons.forEach(button=>button.onclick=()=>show(button.dataset.paymentTab));
 const proofTools=document.createElement('div');proofTools.className='finance-tools';proofTools.style.margin='0 22px 16px';proofTools.innerHTML='<label><span>⌕</span><input id="proof-search" type="search" placeholder="Search business, code or invoice"></label><select id="proof-status"><option value="all">All proof statuses</option><option value="submitted">Awaiting review</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select>';proof.querySelector('header').after(proofTools);function filterProofs(){const q=proofTools.querySelector('input').value.toLowerCase(),s=proofTools.querySelector('select').value;document.querySelectorAll('#proof-rows .proof-row').forEach(row=>{const badge=row.querySelector('.proof-status')?.textContent.trim().toLowerCase();row.hidden=(s!=='all'&&badge!==s)||!row.textContent.toLowerCase().includes(q)})}proofTools.querySelector('input').oninput=filterProofs;proofTools.querySelector('select').onchange=filterProofs;new MutationObserver(()=>{filterProofs();updateCounts()}).observe(document.querySelector('#proof-rows'),{childList:true});
 const settleTools=document.createElement('div');settleTools.className='finance-tools';settleTools.style.margin='0 22px 16px';settleTools.innerHTML='<label><span>⌕</span><input type="search" placeholder="Search mosque settlements"></label>';settlements.querySelector('header').after(settleTools);settleTools.querySelector('input').oninput=e=>document.querySelectorAll('#settlement-grid .settlement-card').forEach(card=>card.hidden=!card.textContent.toLowerCase().includes(e.target.value.toLowerCase()));function updateCounts(){tabs.querySelector('[data-payment-tab="accounts"] b').textContent=document.querySelectorAll('#account-rows tr').length;tabs.querySelector('[data-payment-tab="proofs"] b').textContent=document.querySelectorAll('#proof-rows .proof-row').length;tabs.querySelector('[data-payment-tab="settlements"] b').textContent=document.querySelectorAll('#settlement-grid .settlement-card').length}new MutationObserver(updateCounts).observe(document.querySelector('#account-rows'),{childList:true});new MutationObserver(updateCounts).observe(document.querySelector('#settlement-grid'),{childList:true});updateCounts();show(active);
})();
