document.querySelector('#forgot-form').onsubmit=async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button'),message=document.querySelector('#forgot-message');button.disabled=true;try{const response=await fetch('/api/auth/password-reset/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.elements.email.value})}),result=await response.json();message.textContent=result.message||'If an account matches that email, a reset link has been sent.';message.classList.add('success');message.hidden=false;form.elements.email.disabled=true}catch(_){message.textContent='The request could not be completed. Please try again.';message.hidden=false}finally{button.disabled=false}};
