(async function(){const response=await fetch('/api/finance/audit'),result=await response.json(),items=result.items||[],section=document.createElement('section');section.className='admin-users-card admin-audit-card';const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),format=value=>value&&Object.keys(value).length?Object.entries(value).map(([key,item])=>`${key}: ${item}`).join(' · '):'—';section.innerHTML=`<header class="admin-audit-head"><div><p class="eyebrow"><span></span> Accountability</p><h2>Admin audit log</h2><p>Approvals, account controls, payment decisions, pricing changes and administrator management.</p></div><input type="search" placeholder="Search action, admin or account"></header><div class="admin-audit-list">${items.map(item=>`<article><div><strong>${esc(item.action.replace(/[._]/g,' '))}</strong><small>${esc(item.entityType)} · ${esc(item.entityId)}</small></div><div><strong>${esc(item.actor)}</strong><small>${new Date(item.createdAt).toLocaleString('en-GB')}</small></div><div><small>Previous</small><span>${esc(format(item.before))}</span></div><div><small>New</small><span>${esc(format(item.after))}</span></div><p>${esc(item.reason||'No administrator note')}</p></article>`).join('')||'<p class="empty-audit">No administrator actions have been recorded yet.</p>'}</div>`;document.querySelector('.dashboard-content').append(section);section.querySelector('input').oninput=event=>section.querySelectorAll('.admin-audit-list article').forEach(row=>row.hidden=!row.textContent.toLowerCase().includes(event.target.value.toLowerCase()))})();
