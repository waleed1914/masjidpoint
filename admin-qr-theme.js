(function(){const style=document.createElement('link');style.rel='stylesheet';style.href='admin-qr-islamic.css?v=1';document.head.appendChild(style);function apply(){document.querySelectorAll('img[src*="create-qr-code"]').forEach(image=>{const card=image.closest('.view-card');if(!card||card.classList.contains('islamic-qr-card'))return;card.classList.add('islamic-qr-card');const mark=document.createElement('div');mark.className='qr-islamic-mark';mark.setAttribute('aria-hidden','true');mark.textContent='☾';image.before(mark);const body=image.closest('.view-card-body'),name=image.nextElementSibling,description=name?.nextElementSibling,actions=description?.nextElementSibling,url=actions?.nextElementSibling;if(name)name.classList.add('qr-masjid-name');if(description)description.classList.add('qr-description');if(actions)actions.classList.add('qr-actions');if(url)url.classList.add('qr-url')})}apply();new MutationObserver(apply).observe(document.body,{childList:true,subtree:true})})();
