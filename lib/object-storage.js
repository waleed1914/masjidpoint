const crypto=require('crypto'),{S3Client,PutObjectCommand,GetObjectCommand,DeleteObjectCommand}=require('@aws-sdk/client-s3'),{getSignedUrl}=require('@aws-sdk/s3-request-presigner');
const rules={payment_proof:{max:5242880,mimes:new Set(['image/jpeg','image/png','application/pdf'])},cv:{max:5242880,mimes:new Set(['application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document'])},document:{max:10485760,mimes:new Set(['image/jpeg','image/png','application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document'])}};
class PrivateObjectStorage{constructor(){this.bucket=process.env.OBJECT_STORAGE_BUCKET;this.client=this.bucket?new S3Client({region:process.env.OBJECT_STORAGE_REGION||'auto',endpoint:process.env.OBJECT_STORAGE_ENDPOINT,forcePathStyle:process.env.OBJECT_STORAGE_FORCE_PATH_STYLE==='true',credentials:{accessKeyId:process.env.OBJECT_STORAGE_ACCESS_KEY_ID||'',secretAccessKey:process.env.OBJECT_STORAGE_SECRET_ACCESS_KEY||''}}):null}assertConfigured(){if(!this.client||!this.bucket)throw Error('Private object storage is not configured.')}validate(kind,mime,size){const r=rules[kind];if(!r)throw Object.assign(Error('Unsupported document kind.'),{status:400});if(!r.mimes.has(mime))throw Object.assign(Error('This file type is not allowed.'),{status:415});if(size<=0||size>r.max)throw Object.assign(Error(`File exceeds the ${r.max/1048576} MB limit.`),{status:413})}async put({kind,ownerType,ownerId,name,mime,buffer}){this.assertConfigured();this.validate(kind,mime,buffer.length);const id=crypto.randomUUID(),safe=String(name||'document').replace(/[^a-zA-Z0-9._-]/g,'_').slice(-100),objectKey=`private/${ownerType}/${encodeURIComponent(ownerId)}/${kind}/${id}-${safe}`,sha256=crypto.createHash('sha256').update(buffer).digest('hex');await this.client.send(new PutObjectCommand({Bucket:this.bucket,Key:objectKey,Body:buffer,ContentType:mime,ServerSideEncryption:process.env.OBJECT_STORAGE_SSE||'AES256',Metadata:{documentId:id,sha256}}));return{id,ownerType,ownerId,kind,objectKey,originalName:name,mimeType:mime,size:buffer.length,sha256}}async signedRead(key,name){this.assertConfigured();return getSignedUrl(this.client,new GetObjectCommand({Bucket:this.bucket,Key:key,ResponseContentDisposition:`inline; filename="${String(name).replace(/["\r\n]/g,'_')}"`}),{expiresIn:120})}async remove(key){this.assertConfigured();await this.client.send(new DeleteObjectCommand({Bucket:this.bucket,Key:key}))}}
module.exports={PrivateObjectStorage,rules};
