(function(){if(new URLSearchParams(location.search).has('status'))return;const page=(location.pathname.split('/').pop()||'').replace(/\.html$/,''),target=page==='admin-applications'?'pending':['admin-masjids','admin-businesses'].includes(page)?'approved':null;if(!target)return;let attempts=0;function select(){const button=document.querySelector(`[data-status="${target}"]`);if(button&&typeof button.onclick==='function'){button.click();return}if(attempts++<40)setTimeout(select,50)}select()})();
