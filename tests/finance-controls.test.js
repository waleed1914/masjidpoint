const assert=require('assert'),{cancelInvoice,refund,csv}=require('../lib/finance-service');
const db={masjidPointFinance:{accounts:[{code:'BUS-TEST',name:'Test Business',invoices:[{number:'INV-DUE',amount:20,paid:0,lines:[]},{number:'INV-PAID',amount:30,paid:30,lines:[]}],payments:[]}],audit:[],settlementHistory:[],settled:{}}};
cancelInvoice(db,{code:'BUS-TEST',number:'INV-DUE',reason:'Duplicate invoice',actor:'Test Admin'});assert.equal(db.masjidPointFinance.accounts[0].invoices[0].status,'cancelled');assert.equal(db.masjidPointFinance.audit[0].action,'invoice.cancelled');assert.throws(()=>cancelInvoice(db,{code:'BUS-TEST',number:'INV-PAID',reason:'Wrong',actor:'Test Admin'}),/Refund/);
refund(db,{code:'BUS-TEST',number:'INV-PAID',amount:10,reason:'Service withdrawn',transactionReference:'RF-100',actor:'Test Admin'});assert.equal(db.masjidPointFinance.accounts[0].invoices[1].paid,20);assert.equal(db.masjidPointFinance.accounts[0].invoices[1].refundedTotal,10);assert.match(csv(db,'invoices'),/partially_refunded/);assert.match(csv(db,'audit'),/payment.refunded/);console.log('PASS financial controls');
