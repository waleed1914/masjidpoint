(async function(){const root=document.querySelector('#masjid-view');if(!root)return;const reference=new URLSearchParams(location.search).get('reference'),state=await MasjidDB.state(),mosque=(state.masjidPointAdminApplications||[]).find(item=>item.reference===reference&&item.type==='masjid');if(!mosque||!['approved','activated'].includes(mosque.status))return;const bank=mosque.donationBankDetails||{},card=document.createElement('section');card.className='view-card mosque-donation-admin';card.innerHTML=`<header><div><p class="eyebrow"><span></span> Public donations</p><h2>Mosque donation account</h2></div><a href="donations?masjid=${encodeURIComponent(reference)}" target="_blank" rel="noopener">Preview donation page ↗</a></header><div class="view-card-body"><p class="donation-admin-intro">These details appear publicly when someone selects this mosque on the donation page.</p><form id="mosque-donation-form"><label class="donation-active"><input name="active" type="checkbox" ${bank.active?'checked':''}> Show this mosque on the donation page</label><div class="donation-admin-grid"><label>Account name<input name="accountName" value="${esc(bank.accountName)}" required></label><label>Bank name<input name="bankName" value="${esc(bank.bankName)}" required></label><label>Sort code<input name="sortCode" value="${esc(bank.sortCode)}" placeholder="12-34-56" inputmode="numeric" required></label><label>Account number<input name="accountNumber" value="${esc(bank.accountNumber)}" placeholder="12345678" inputmode="numeric" required></label><label class="wide">IBAN <small>(optional)</small><input name="iban" value="${esc(bank.iban)}"></label><label class="wide">Payment reference <small>(optional)</small><input name="reference" value="${esc(bank.reference)}" placeholder="For example: General donation"></label><label class="wide">Donation message <small>(optional)</small><textarea name="message" maxlength="500" placeholder="A short message shown to donors">${esc(bank.message)}</textarea></label></div><p class="pricing-error" id="donation-settings-error" hidden></p><button class="button" type="submit">Save donation details</button></form></div>`;const left=root.querySelector('.masjid-view-grid>div');left.appendChild(card);card.querySelector('form').onsubmit=async event=>{event.preventDefault();const form=event.currentTarget,data=new FormData(form),button=form.querySelector('[type=submit]');button.disabled=true;const response=await fetch('/api/admin/mosque-donation-settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({masjidReference:reference,active:form.elements.active.checked,accountName:data.get('accountName'),bankName:data.get('bankName'),sortCode:data.get('sortCode'),accountNumber:data.get('accountNumber'),iban:data.get('iban'),reference:data.get('reference'),message:data.get('message')})}),result=await response.json();if(!response.ok){const error=card.querySelector('#donation-settings-error');error.textContent=result.error;error.hidden=false;button.disabled=false;return}location.reload()};function esc(value){return String(value||'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}})();
