(function(){const input=document.querySelector('#proof-file'),drop=document.querySelector('.proof-drop'),form=document.querySelector('#proof-form');if(!input||!drop||!form)return;const submit=form.querySelector('button[type="submit"]'),feedback=document.createElement('div');feedback.className='proof-file-feedback';feedback.hidden=true;drop.after(feedback);function clear(){feedback.hidden=true;feedback.innerHTML='';drop.classList.remove('proof-selected');if(!submit.disabled)submit.textContent='Submit for verification'}function size(bytes){return bytes<1024*1024?`${Math.max(1,Math.round(bytes/1024))} KB`:`${(bytes/1024/1024).toFixed(1)} MB`}function show(file){if(!file)return clear();drop.classList.add('proof-selected');submit.textContent='Submit selected proof →';const image=file.type.startsWith('image/'),source=image?URL.createObjectURL(file):'';feedback.innerHTML=`${image?`<img src="${source}" alt="Selected payment proof preview">`:'<span class="proof-pdf-icon">PDF</span>'}<div><strong><i>✓</i> Payment proof selected</strong><b>${escapeHtml(file.name)}</b><small>${size(file.size)} · Ready to upload</small></div><button type="button" id="remove-proof-file">Remove</button>`;feedback.hidden=false;feedback.querySelector('#remove-proof-file').onclick=()=>{input.value='';document.querySelector('#proof-file-name').textContent='Choose payment evidence';clear()}}input.addEventListener('change',()=>{const file=input.files[0];if(!file)return clear();if(file.size>5*1024*1024)return clear();show(file)});form.addEventListener('reset',()=>setTimeout(clear));document.addEventListener('click',event=>{if(event.target.closest('[data-own-proof],#start-proof,#pay-workflow'))setTimeout(clear)});function escapeHtml(value){return String(value||'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}})();
