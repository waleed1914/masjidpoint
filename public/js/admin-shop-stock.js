(function(){const rows=document.querySelector('#product-rows');if(!rows)return;function enhance(){rows.querySelectorAll('[data-toggle-product]').forEach(toggle=>{if(toggle.parentElement.querySelector('[data-adjust-stock]'))return;const button=document.createElement('button');button.type='button';button.dataset.adjustStock=toggle.dataset.toggleProduct;button.textContent='Stock';button.title='Adjust stock';toggle.before(button)})}new MutationObserver(enhance).observe(rows,{childList:true});enhance();rows.addEventListener('click',async event=>{const button=event.target.closest('[data-adjust-stock]');if(!button)return;const state=await MasjidDB.state(),product=(state.masjidPointProducts||[]).find(x=>x.id===button.dataset.adjustStock);if(!product)return;const value=prompt(`Set global stock for ${product.name}`,product.stock);if(value===null)return;const stock=Number(value);if(!Number.isInteger(stock)||stock<0)return alert('Enter a whole stock quantity of zero or more.');const response=await fetch('/api/admin/product',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:product.id,action:'stock',stock})}),result=await response.json();if(!response.ok)return alert(result.error||'Stock could not be updated.');location.reload()})})();
