(function(){function update(){document.querySelectorAll('.native-admin-pager label').forEach(label=>{if(label.dataset.showLabel)return;const select=label.querySelector('select');label.textContent='';const word=document.createElement('span');word.textContent='Show';label.append(word,select);label.dataset.showLabel='true'})}update();new MutationObserver(update).observe(document.body,{childList:true,subtree:true})})();
