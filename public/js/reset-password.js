document.querySelector('#reset-form').onsubmit=async event=>{event.preventDefault();const form=event.currentTarget,message=document.querySelector('#reset-message'),password=form.elements.password.value;if(password!==form.elements.confirm.value){message.textContent='The passwords do not match.';message.hidden=false;return}const button=form.querySelector('button');button.disabled=true;try{const response=await fetch('/api/auth/password-reset/complete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:new URLSearchParams(location.search).get('token'),password})}),result=await response.json();if(!response.ok)throw Error(result.error);message.textContent='Password updated. You can now sign in.';message.classList.add('success');message.hidden=false;setTimeout(()=>location.href='login',1600)}catch(error){message.textContent=error.message||'This reset link could not be used.';message.hidden=false;button.disabled=false}};
