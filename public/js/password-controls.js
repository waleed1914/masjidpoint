(function(){
  const rules=[['length','At least 12 characters',v=>v.length>=12],['upper','One uppercase letter',v=>/[A-Z]/.test(v)],['lower','One lowercase letter',v=>/[a-z]/.test(v)],['number','One number',v=>/\d/.test(v)],['symbol','One symbol',v=>/[^A-Za-z0-9]/.test(v)]];
  function addEye(input){if(input.parentElement?.classList.contains('password-field-wrap'))return;const wrap=document.createElement('span');wrap.className='password-field-wrap';input.before(wrap);wrap.appendChild(input);const button=document.createElement('button');button.type='button';button.className='password-visibility';button.setAttribute('aria-label','Show password');button.setAttribute('aria-pressed','false');button.textContent='◉';button.onclick=()=>{const visible=input.type==='text';input.type=visible?'password':'text';button.setAttribute('aria-label',visible?'Show password':'Hide password');button.setAttribute('aria-pressed',String(!visible));button.textContent=visible?'◉':'◌'};wrap.appendChild(button)}
  const inputs=[...document.querySelectorAll('input[type="password"]')];inputs.forEach(addEye);const primary=inputs.find(input=>input.autocomplete==='new-password'&&!/confirm/i.test(input.name||''))||inputs.find(input=>/password/i.test(input.name||'')&&!/confirm/i.test(input.name||'')&&input.minLength>=12);if(!primary)return;
  const list=document.createElement('ul');list.className='password-checklist';list.setAttribute('aria-live','polite');list.innerHTML=rules.map(([key,label])=>`<li data-rule="${key}">${label}</li>`).join('');primary.closest('label')?.before(list);const update=()=>rules.forEach(([key,,test])=>list.querySelector(`[data-rule="${key}"]`).classList.toggle('met',test(primary.value)));primary.addEventListener('input',update);update();
})();
